# Regenerated by running the tools -- never commit these.
__pycache__/
*.pyc

# ngspice writes this on every run of sim/dpt.cir
sim/out.dat

# Icarus writes this on every run of the RTL testbench
rtl/*.vcd

# gunzipped copies of the committed .csv.gz sweeps
results/lloop_sweep.csv
results/emi_sweep.csv
